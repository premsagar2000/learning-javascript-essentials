//Arrays

var firstName = "Prem"; // can have only one value

var names = ["Prem", "Sagar", "Aisha", "Mahtab"]; // arrays can store multiple values

console.log(names);
console.log("Index 0 - "+names[0]); // Index starts from 0 in arrays
console.log("Index 1 - "+names[1]);
console.log("Index 2 - "+names[2]);
console.log("Index 3 - "+names[3]);
console.log("Index 4 - "+names[4]); // It is undefined because there is no any fifth item in the array names. It only contains 4 items

console.log("Size of the array names - "+names.length); // Length of an array