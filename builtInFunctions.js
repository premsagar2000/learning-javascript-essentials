var person = {
    name : "Prem"
}

console.log(Object.values(person)); // built in function object.values() 
console.log(Object.keys(person)); // built in function object.keys()

console.log("Prem".toLowerCase());
console.log("Prem".toUpperCase());
console.log("Prem".indexOf('e'));

// There are many other built-in functions in javascript. 
